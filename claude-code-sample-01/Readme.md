# Readme
This application was built only by Claude Code!
I uploaded the design-products-table.png and the rest was generated by Claude Code.

You run it with
```sh
cd product-table
npm install
npm run dev
```

## Build by Claude Code
The following commands were run by Claude Code to generate the code for this application:

```sh
# Scaffold a new React + TypeScript Vite app
npm create vite@latest product-table -- --template react-ts

# Install dependencies
cd product-table && npm install

# Start dev server manually (first attempt)
npm run dev -- --port 5199

# Kill the leftover process at the end
lsof -ti :5199 | xargs kill
```

